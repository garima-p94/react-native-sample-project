import { StyleSheet } from 'react-native';
import { fonts, colors } from './attributes';

export const textStyles = StyleSheet.create({
  heading: {
    fontWeight: '800',
    color: colors.darkGray,
  },
});
